import React, { useState } from 'react';

const BlogPage = () => {
    const [blogs, SetBlog] = useState([])
    return (
        <div>
            Hello
        </div>
    );
};

export default BlogPage;